{
  "name": "notes-app-with-tags",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Notes App with Tags",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
